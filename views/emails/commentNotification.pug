extends layouts/default

block content
  p Здравствуйте, #{firstName},
  p Оставлен новый комментарий. Проверить и одобрить его можно в личном кабинете.
  if comment
    ul
      li=`Автор: ${comment.owner.firstName} ${comment.owner.lastName} -> (${comment.owner.role})`
      li=`Дата: ${comment.createdAt}`
      li Текст комментария:
        p=`${comment.text}`
  table.btn.btn-primary(role='presentation', border='0', cellpadding='0', cellspacing='0')
    tbody
      tr
        td(align='left')
          table(role='presentation', border='0', cellpadding='0', cellspacing='0')
            tbody
              tr
                td
                  a(href=`${url}`, target='_blank') Перейти к коментарию
  p - С уважением, команда CraftHub